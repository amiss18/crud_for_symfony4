{# app/Resources/views/base.html.twig #}
{% extends "base.html.twig" %}
{% form_theme form 'bootstrap_4_layout.html.twig' %}


{% block body %}
    <h1 class="my-4">add product</h1>

      <div class="row">
            <div class="col-md-8">
                {{ form_start(form, {'action': path('product_save')}) }}


                {{ form_label(form.price) }}
                {{ form_widget(form.price, { 'type':'number' }) }}

                {{ form_widget(form) }}


                {{ form_end(form) }}
            </div>
        </div>

{% endblock body %}

